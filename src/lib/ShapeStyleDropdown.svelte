<script>
    let { opacity = $bindable(), blur = $bindable(), blendMode = $bindable() } = $props()
</script>

<!-- probably should make a base dropdown component for all of these use, but this will do for now -->

<label>
    Shape
    <button>v</button>
</label>