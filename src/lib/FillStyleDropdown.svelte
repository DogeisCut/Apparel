<script>
    let { color = $bindable(), fillRule = $bindable(), opacity = $bindable()} = $props()
</script>

<!-- probably should make a base dropdown component for all of these use, but this will do for now -->
 
<label>
    Fill
    <button>v</button>
</label>